# Lucas Cleminson — Portfolio

A modern, responsive portfolio built with React and TypeScript.

## Development

```sh
npm install
npm run dev
npm run build
```

## Deployment

This project is configured to be hosted on GitHub Pages. To deploy to GitHub Pages:

```sh
npm run deploy
```

The site will be deployed to the `gh-pages` branch.
